<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!-- MainActivity: fondo de ventana visible detrás del área gestual inferior. -->
    <style name="MainActivityTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/app_window_background_color</item>
        <item name="android:navigationBarColor">@color/app_navigation_bar_color</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- Splash: fondo #685CF0 mientras carga la app nativa. -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:navigationBarColor">@color/app_navigation_bar_color</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- SplashActivity: solo fondo para evitar parpadeo antes del layout -->
    <style name="SplashActivityTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/splash_background_color</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
